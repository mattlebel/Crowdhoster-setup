<!DOCTYPE html>
<html>
<head>
  <title>Crowdhoster by Crowdtilt | Setup & Customization Guide</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Setup and customize your own Crowdfunding campaign">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<%= render 'partials/nav' %>

<div id="wrap">

	<div class="container">
	  <div class="row-fluid">
	    <div class="span12">
	      <img src="assets/crowdtilt.png">
	      <hr>
		<% if browser.mobile? %>
		    <%= render 'partials/redirect' %>
		<% else %>
			<%= yield %>
		<% end %>
	    </div>
	  </div>
	</div>

</div>

</body>
</html>